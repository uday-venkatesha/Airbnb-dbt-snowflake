version: 2

models:
  - name: bronze_listings
    config:
      materialized: table
      
  - name: bronze_bookings
    config:
      materialized: table
      
  - name: bronze_hosts
    config:
      materialized: table